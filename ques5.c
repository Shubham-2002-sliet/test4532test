#include<stdio.h>
int main()
{
	char a[100] = "welcome students";
	int length;
	printf(" Enter a string to calculate its length \n");
	length = strlen(a);
	printf("length of the string = %d\n",length);
	return 0;
}
